import { AverageColorDirective } from './average-color.directive';

describe('AverageColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AverageColorDirective();
    expect(directive).toBeTruthy();
  });
});
